---
description: "Record a bug or problem to DevLog"
model: default
---

Use the `devlog_write_record` MCP tool with `symbol` set to the configured problem symbol ("×" for unicode or "x" for ascii).
Pass the user's bug description as `content`.
