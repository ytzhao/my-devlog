---
description: "Record a bug or problem to DevLog"
model: default
---

Read `.devlog/config.md` to get the DevLog root path and symbol scheme.
Locate `daily/YYYY-MM-DD.md`, append with `[HH:MM] ×` (or `x`).
Ask for solution if user provides one, append `→ Solution: ...` continuation line.
Ask if沉淀 to `topics/bugfix/` is needed.
Infer project name from current directory.
