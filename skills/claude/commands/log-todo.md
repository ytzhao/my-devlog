---
description: "Add a todo item to DevLog"
model: default
---

Use the `devlog_write_record` MCP tool with `symbol` set to the configured todo symbol ("○" for unicode or "[ ]" for ascii).
Pass the user's task description as `content`.
