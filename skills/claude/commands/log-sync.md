---
description: "Trigger DevLog sync manually"
model: default
---

1. Use `devlog_archive_inbox` to archive inbox records.
2. Use `devlog_sync` to sync today's daily log to projects/.
3. Use `devlog_generate_stats` to regenerate today's statistics table.
Report the results to the user.
